var documenterSearchIndex = {"docs":
[{"category":"section","location":"support/#1.0-Support-Policy","page":"Support Policy","text":"Postgres.jl 1.0 supports Julia 1.10 and later. The release test matrix covers PostgreSQL 14 through 18 on TCP connections. Unix-domain sockets are not supported.","title":"1.0 Support Policy"},{"category":"section","location":"support/#TLS","page":"Support Policy","text":"sslmode=verify-full verifies the certificate chain and server name. Use a DNS name as host, or set sslservername to the DNS name when dialing a resolved address. IP-address subject-alternative-name matching on a TLS 1.2-only server is not supported in 1.0.\n\nClient certificates require both sslcert and sslkey. Postgres.jl 1.0 limits client-certificate connections to TLS 1.2 because the current Reseau 1.x mixed TLS 1.2/1.3 client path does not send the certificate reliably. Server-only TLS connections can negotiate TLS 1.2 or TLS 1.3.\n\nconnect_timeout bounds the TCP connection and TLS handshake. It does not bound PostgreSQL authentication or query execution. Use statement_timeout for query execution on a direct or session-pooled connection.\n\nKeep a manual transaction or streaming cursor on the task that created it. Do not run unrelated operations on that connection until the scope ends. Use ConnectionPool to give concurrent tasks separate connections.","title":"TLS"},{"category":"section","location":"support/#Transaction-Poolers","page":"Support Policy","text":"Connection-form DBInterface.execute(conn, sql, params) is safe through a transaction-mode PgBouncer endpoint. Postgres.jl sends each unnamed extended query as one dependent protocol segment. Explicit named prepared statements require PgBouncer prepared-statement tracking, such as max_prepared_statements > 0.\n\nA transaction pooler does not preserve session state between logical clients. Do not use connection-level statement_timeout, set_statement_timeout!, LISTEN, temporary tables, session advisory locks, or arbitrary SET state in transaction mode. Configure PostgreSQL or the pooler defaults with DateStyle=ISO and IntervalStyle=postgres; Postgres.jl needs these text formats for correct decoding. Use direct connections or session pooling when the application needs session state.\n\nset_statement_timeout! is rejected while a transaction is open. This keeps the durable reconnect setting consistent with PostgreSQL's transactional SET semantics.","title":"Transaction Poolers"},{"category":"section","location":"support/#Types","page":"Support Policy","text":"Built-in scalar types, byte arrays, and one-dimensional PostgreSQL arrays can be bound as parameters. Custom enum, composite, and range registration is a result-decoding feature in 1.0. Bind a text representation with an explicit SQL cast when writing those custom values. Multidimensional Julia arrays are not a supported parameter form in 1.0.","title":"Types"},{"category":"section","location":"support/#Native-Compilation","page":"Support Policy","text":"JuliaC --trim compilation is not supported in Postgres.jl 1.0. Normal Julia package precompilation is supported and is part of CI.","title":"Native Compilation"},{"category":"section","location":"manual/#Manual","page":"Manual","text":"Postgres.jl intentionally keeps its export surface small: using Postgres re-exports DBInterface, while package-specific APIs are accessed through the Postgres. namespace.","title":"Manual"},{"category":"section","location":"manual/#Connecting","page":"Manual","text":"Use DBInterface.connect with either a PostgreSQL URI, a libpq-style keyword string, or explicit host/user/password arguments.\n\nusing Postgres, Tables\n\nconn = DBInterface.connect(\n    Postgres.Connection,\n    \"postgresql://postgres:postgres@127.0.0.1:5432/postgres?sslmode=disable\",\n)\n\nDBInterface.close!(conn)\n\nKeyword DSNs and environment defaults are also supported.\n\nconn = DBInterface.connect(\n    Postgres.Connection,\n    \"host=127.0.0.1 port=5432 user=postgres password=postgres dbname=postgres\",\n)","title":"Connecting"},{"category":"section","location":"manual/#Querying","page":"Manual","text":"DBInterface.execute returns a Tables.jl-compatible result. For small result sets, Tables.rowtable is a convenient way to materialize rows.\n\nrows = Tables.rowtable(DBInterface.execute(conn, \"SELECT 1 AS id, 'hello' AS label\"))\nrow = only(rows)\n@show row.id row.label\n\nThe result object can also be iterated directly.\n\nfor row in DBInterface.execute(conn, \"SELECT generate_series(1, 3) AS n\")\n    @show row.n\nend\n\nPostgreSQL command completion metadata is available on the result.\n\nresult = DBInterface.execute(conn, \"UPDATE items SET seen = true WHERE seen = false\")\n@show Postgres.command_tag(result)\n@show Postgres.rows_affected(result)","title":"Querying"},{"category":"section","location":"manual/#Typed-Results-With-StructUtils","page":"Manual","text":"Postgres.jl result sets implement the StructUtils.jl interface. That means DBInterface.execute can deserialize rows directly into a target type instead of first materializing Tables.rowtable rows.\n\nFor a single-row query, pass a concrete struct type as the fourth argument. The query should return exactly one row.\n\nusing Postgres, StructUtils\n\nstruct CountRow\n    count::Int\nend\n\nrow = DBInterface.execute(conn, \"SELECT count(*)::int AS count FROM users\", (), CountRow)\n@show row.count\n\nFor multi-row queries, pass a vector type.\n\nstruct UserName\n    id::Int\n    name::String\nend\n\nusers = DBInterface.execute(conn, \"SELECT id, name FROM users ORDER BY id\", (), Vector{UserName})\n\nStructUtils field tags let Julia models keep idiomatic field names while SQL keeps idiomatic column names. Tags for Postgres.jl live under the postgres namespace.\n\nusing Dates, Postgres, StructUtils\n\nStructUtils.@tags struct ProfileSummary\n    profileId::Int &(postgres=(name=:profile_id,),)\n    firstName::Union{Missing, String} &(postgres=(name=:first_name,),)\n    lastName::Union{Missing, String} &(postgres=(name=:last_name,),)\n    createdAt::DateTime &(postgres=(name=:created_at,),)\nend\n\nprofile = DBInterface.execute(conn, raw\"\"\"\n    SELECT profile_id, first_name, last_name, created_at\n    FROM profiles\n    WHERE profile_id = $1\n    \"\"\", (profile_id,), ProfileSummary)\n\nprofiles = DBInterface.execute(conn, \"\"\"\n    SELECT profile_id, first_name, last_name, created_at\n    FROM profiles\n    ORDER BY created_at DESC\n    LIMIT 10\n    \"\"\", (), Vector{ProfileSummary})\n\nThe postgres=(name=:column_name,) tag is only needed when a column should map to a differently named field. Columns such as id or name can be left untagged because they already match the Julia field name.","title":"Typed Results With StructUtils"},{"category":"section","location":"manual/#Driver-Styles","page":"Manual","text":"Connection behavior such as query logging, server notices, and asynchronous notifications is selected by a concrete driver style. Subtype Postgres.AbstractPostgresStyle, overload the documented behavior hooks for that style, and pass an instance with the style connection keyword. The default Postgres.PostgresStyle keeps query logging disabled and reports server notices through Julia's logger.\n\nstruct LoggingStyle <: Postgres.AbstractPostgresStyle end\nPostgres.query_logging_enabled(::LoggingStyle) = true\nPostgres.query_logger(::LoggingStyle, event::Symbol, info::NamedTuple) = @info \"query\" event info.success info.duration_ns\nPostgres.notice_callback(::LoggingStyle, notice) = @info \"notice\" notice\nPostgres.notification_callback(::LoggingStyle, notification) = @info \"notification\" notification\n\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1 user=postgres dbname=postgres\"; style=LoggingStyle())\n\nquery_logger's info includes the SQL and the bound parameter values, so a logger that writes them out records whatever sensitive data those queries carry. Redact or omit info.params when the log destination is less trusted than the database itself.","title":"Driver Styles"},{"category":"section","location":"manual/#Parameters-And-Prepared-Statements","page":"Manual","text":"Use PostgreSQL placeholders ($1, $2, ...) and pass a tuple or other iterable of parameter values. Note the use of raw\"...\" strings so that $1 is not treated as Julia string interpolation.\n\nrows = Tables.rowtable(DBInterface.execute(conn, raw\"SELECT $1::int + $2::int AS total\", (20, 22)))\n@show only(rows).total\n\nPrepared statements can be created explicitly. Postgres.jl caches their named backend statements with LRU eviction while returning an independent handle to each caller. Set statement_cache_maxsize=0 to disable this cache.\n\nstmt = DBInterface.prepare(conn, raw\"SELECT $1::text AS value\")\nrows = Tables.rowtable(DBInterface.execute(stmt, (\"prepared\",)))\nDBInterface.close!(stmt)\n\nBulk inserts can use DBInterface.executemany.\n\nDBInterface.execute(conn, \"CREATE TEMP TABLE demo_many (id int, name text)\")\nstmt = DBInterface.prepare(conn, raw\"INSERT INTO demo_many VALUES ($1, $2)\")\nDBInterface.executemany(stmt, ([1, 2, 3], [\"a\", \"b\", \"c\"]))\nDBInterface.close!(stmt)","title":"Parameters And Prepared Statements"},{"category":"section","location":"manual/#Transactions","page":"Manual","text":"Postgres.jl supports both its connection-passing helper and the DBInterface transaction API.\n\nPostgres.transaction(conn) do tx\n    DBInterface.execute(tx, \"CREATE TEMP TABLE tx_demo (id int)\")\n    DBInterface.execute(tx, \"INSERT INTO tx_demo VALUES (1)\")\nend\n\nDBInterface.transaction(conn) do\n    DBInterface.execute(conn, \"INSERT INTO tx_demo VALUES (2)\")\nend\n\nNested transactions use savepoints.","title":"Transactions"},{"category":"section","location":"manual/#Streaming-Results","page":"Manual","text":"For larger result sets, Postgres.cursor fetches rows in batches.\n\ncur = Postgres.cursor(conn, \"SELECT generate_series(1, 1000) AS n\"; fetchsize=100)\ntry\n    for row in cur\n        @show row.n\n    end\nfinally\n    DBInterface.close!(cur)\nend","title":"Streaming Results"},{"category":"section","location":"manual/#COPY","page":"Manual","text":"Use Postgres.copy_from and Postgres.copy_to for PostgreSQL's COPY protocol.\n\nDBInterface.execute(conn, \"CREATE TEMP TABLE copy_demo (id int, name text)\")\nPostgres.copy_from(conn, \"COPY copy_demo (id, name) FROM STDIN\", \"1\\talpha\\n2\\tbeta\\n\")\nbytes = Postgres.copy_to(conn, \"COPY copy_demo TO STDOUT\")","title":"COPY"},{"category":"section","location":"manual/#Cancellation","page":"Manual","text":"Postgres.cancel_query! sends a PostgreSQL CancelRequest for a running query on another task.\n\ntask = Threads.@spawn DBInterface.execute(conn, \"SELECT pg_sleep(10)\")\nsleep(0.5)\nPostgres.cancel_query!(conn)\n\nThe running query raises a Postgres.Error with PostgreSQL SQLSTATE 57014.","title":"Cancellation"},{"category":"section","location":"manual/#LISTEN-And-NOTIFY","page":"Manual","text":"Use one connection to listen and another to notify.\n\nlistener = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1 user=postgres password=postgres dbname=postgres\")\nnotifier = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1 user=postgres password=postgres dbname=postgres\")\n\nPostgres.listen!(listener, \"events\")\nPostgres.notify!(notifier, \"events\", \"hello\")\nnotification = Postgres.wait_for_notification(listener; timeout=5.0)\n@show notification.channel notification.payload\n\nDBInterface.close!(notifier)\nDBInterface.close!(listener)","title":"LISTEN And NOTIFY"},{"category":"section","location":"manual/#Type-Translation","page":"Manual","text":"Postgres.jl maps common PostgreSQL types to Julia values:\n\nintegers, floats, booleans, text, UUIDs, dates, times, timestamps, and bytea map to their natural Julia types.\njson and jsonb are returned as lazy JSON values from JSON.jl.\nnumeric maps to Postgres.Numeric to preserve decimal scale.\ninterval maps to Dates.Period or Dates.CompoundPeriod.\narrays map to Julia arrays, preserving missing for SQL NULL.\nPostgreSQL range types map to Postgres.PostgresRange{T}.\n\nCustom enum, composite, and range types can be registered on a connection.\n\nDBInterface.execute(conn, \"CREATE TYPE mood AS ENUM ('sad', 'ok', 'happy')\")\nPostgres.register_enum!(conn, \"mood\"; julia_type=Symbol)\n\nrow = only(Tables.rowtable(DBInterface.execute(conn, \"SELECT 'happy'::mood AS mood\")))\n@show row.mood\n\nRegistering composite and range types follows the same pattern.\n\nRegistration controls result decoding. Direct parameter binding for registered enum, composite, and range Julia values is not part of the 1.0 interface. Bind their PostgreSQL text representation and add an explicit SQL cast when needed.","title":"Type Translation"},{"category":"section","location":"manual/#Session-Formats","page":"Manual","text":"Text decoding assumes the server renders dates and timestamps in the ISO DateStyle output format and intervals in the postgres IntervalStyle. The driver checks the server-reported settings at connect time and issues a SET for any that differ, preserving the configured date field order (MDY/DMY/ YMD) since it decides how ambiguous input literals like '01/02/2020' are read. The alignment is re-applied on reconnect, but not if the session is changed afterwards: running SET DateStyle = ... or SET IntervalStyle = ... mid-session breaks decoding — intervals and unparseable dates raise errors rather than silently returning wrong values.\n\nTransaction-mode poolers cannot preserve session settings between logical connections. See the 1.0 Support Policy before using this mode.","title":"Session Formats"},{"category":"section","location":"manual/#Values-Without-A-Julia-Representation","page":"Manual","text":"A few PostgreSQL values have no faithful Julia equivalent and raise Postgres.PostgresInterfaceError when decoded rather than silently returning a wrong value: infinity/-infinity dates and timestamps, dates in the BC era, and numeric NaN/infinity. \"char\" columns (the 1-byte internal catalog type) decode to Char, including the zero byte ('\\0') and high-bit bytes; note that a '\\0' read from such a column cannot be bound back as a text parameter, because PostgreSQL rejects NUL bytes in text — write it with an explicit cast such as 0::\"char\" instead.","title":"Values Without A Julia Representation"},{"category":"type","location":"manual/#Postgres.API.AbstractPostgresStyle","page":"Manual","text":"AbstractPostgresStyle <: StructUtils.StructStyle\n\nStyle hierarchy for customizing driver behavior, following the StructUtils \"style\" pattern. Pass a custom style to Connection(; style=MyStyle()) and overload the behavior interface on it:\n\nPostgres.query_logging_enabled(::MyStyle) = true\nPostgres.query_logger(::MyStyle, event::Symbol, info::NamedTuple) = ...\nPostgres.notice_callback(::MyStyle, notice) = ...\nPostgres.notification_callback(::MyStyle, notification) = ...\n\nquery_logger's info carries the SQL and the bound parameter values, so a logger that writes them out will record whatever sensitive data those queries carry — redact or omit info.params if the log is not as trusted as the database.\n\nCustom styles inherit the default row-materialization traits (lift/structlike/...), which dispatch on AbstractPostgresStyle, and are used as the StructUtils style when materializing query results — so StructUtils.lift overloads on a custom style apply to row values too. Static dispatch on the style (rather than Function-typed callback fields) also keeps the driver compilable under juliac --trim.\n\n\n\n\n\n","title":"Postgres.API.AbstractPostgresStyle"},{"category":"type","location":"manual/#Postgres.API.PostgresStyle","page":"Manual","text":"The default style: no query logging, NOTICE messages surface as @warn.\n\n\n\n\n\n","title":"Postgres.API.PostgresStyle"},{"category":"function","location":"manual/#Postgres.API.query_logging_enabled","page":"Manual","text":"Postgres.query_logging_enabled(style) -> Bool\n\nWhether query_logger should be called for queries on connections using style. false by default, which also skips the timing work the logger would need.\n\n\n\n\n\n","title":"Postgres.API.query_logging_enabled"},{"category":"function","location":"manual/#Postgres.API.query_logger","page":"Manual","text":"Postgres.query_logger(style, event::Symbol, info::NamedTuple)\n\nCalled after each query when query_logging_enabled is true for style. event is :execute, :copy_from, or :copy_to; info carries sql, duration_ns, success, the bound params (for :execute), and error when the query failed.\n\ninfo.params holds the query's parameter values, so a logger that records them will record whatever sensitive data those queries carry.\n\n\n\n\n\n","title":"Postgres.API.query_logger"},{"category":"function","location":"manual/#Postgres.API.notice_callback","page":"Manual","text":"Postgres.notice_callback(style, notice)\n\nCalled for each NoticeResponse the server sends. notice is a Dict of the raw notice fields, keyed by their single-character protocol codes (\"M\" is the message, \"S\" the severity). Emits the message as a @warn by default.\n\n\n\n\n\n","title":"Postgres.API.notice_callback"},{"category":"function","location":"manual/#Postgres.API.notification_callback","page":"Manual","text":"Postgres.notification_callback(style, notification::Notification)\n\nCalled for each asynchronous NOTIFY (Notification) received while reading query results. Does nothing by default.\n\nA connection only observes notifications while it is reading from the server, so a connection that is idle or busy in another phase of a query may not see one. Use wait_for_notification on a dedicated connection to receive every notification on a channel.\n\n\n\n\n\n","title":"Postgres.API.notification_callback"},{"category":"section","location":"#Postgres.jl","page":"Home","text":"Postgres.jl is a PostgreSQL client that speaks the v3 wire protocol with DBInterface and Tables integration.\n\nSee the Manual for a guided walk through connections, queries, prepared statements, transactions, cancellation, notifications, and type translation. See the 1.0 Support Policy for tested versions and explicit limits.","title":"Postgres.jl"},{"category":"section","location":"#Installation","page":"Home","text":"import Pkg\nPkg.add(\"Postgres\")","title":"Installation"},{"category":"section","location":"#Connection-options","page":"Home","text":"Postgres.jl accepts DSN strings or PostgreSQL URIs and supports:\n\nlibpq-style keyword strings such as host=127.0.0.1 port=5432 user=postgres dbname=postgres.\nEnvironment defaults from PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE, PGAPPNAME, PGCONNECT_TIMEOUT, and TLS-related PGSSL* variables.\nsslmode values: disable, prefer (the default), require, verify-full. Only verify-full verifies the server's certificate; require encrypts without authenticating the server, and the default prefer falls back to an unencrypted connection if the server declines TLS. Use verify-full with sslrootcert when the connection needs to be authenticated.\nTLS files: sslrootcert, sslcert, sslkey, and sslcapath (sslcapath is a fallback CA bundle or directory, used only when sslrootcert is unset and ignored otherwise). sslservername overrides the TLS server name when connecting to a pre-resolved address; under verify-full it is also the name the certificate is verified against, so it must name the server you intend to authenticate.\nconnect_timeout (seconds), statement_timeout (milliseconds).\napplication_name and statement_cache_maxsize.\n\nOptions that request unsupported security or server-selection behavior are rejected. They are not silently ignored.\n\nusing Postgres, DBInterface\nconn = DBInterface.connect(Postgres.Connection, \"postgresql://postgres:postgres@127.0.0.1:5432/postgres?sslmode=disable\")\nDBInterface.close!(conn)","title":"Connection options"},{"category":"section","location":"#Query-execution","page":"Home","text":"using Postgres, DBInterface, Tables\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\")\nrows = Tables.rowtable(DBInterface.execute(conn, raw\"SELECT $1::int AS val\", (42,)))\n@show rows[1].val\nDBInterface.close!(conn)","title":"Query execution"},{"category":"section","location":"#StructUtils-results","page":"Home","text":"Postgres.jl integrates with StructUtils.jl, so query results can be materialized directly as Julia structs.\n\nusing Postgres, DBInterface, StructUtils\n\nstruct CountRow\n    count::Int\nend\n\nrow = DBInterface.execute(conn, \"SELECT count(*)::int AS count FROM users\", (), CountRow)\n@show row.count\n\nWhen PostgreSQL column names do not match Julia field names, add field tags in the postgres namespace. Postgres.jl's StructUtils style uses those tags while deserializing rows.\n\nusing Dates, Postgres, DBInterface, StructUtils\n\nStructUtils.@tags struct ProfileSummary\n    profileId::Int &(postgres=(name=:profile_id,),)\n    firstName::Union{Missing, String} &(postgres=(name=:first_name,),)\n    lastName::Union{Missing, String} &(postgres=(name=:last_name,),)\n    createdAt::DateTime &(postgres=(name=:created_at,),)\nend\n\nprofile = DBInterface.execute(conn, raw\"\"\"\n    SELECT profile_id, first_name, last_name, created_at\n    FROM profiles\n    WHERE profile_id = $1\n    \"\"\", (profile_id,), ProfileSummary)\n\nprofiles = DBInterface.execute(conn, \"\"\"\n    SELECT profile_id, first_name, last_name, created_at\n    FROM profiles\n    ORDER BY created_at DESC\n    LIMIT 10\n    \"\"\", (), Vector{ProfileSummary})\n\nExplicit named prepared statements use an LRU backend cache; disable it via statement_cache_maxsize=0.\n\nusing Postgres, DBInterface, Tables\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\"; statement_cache_maxsize=5)\nstmt = DBInterface.prepare(conn, raw\"SELECT $1::int AS val\")\nrows = Tables.rowtable(DBInterface.execute(stmt, (7,)))\nDBInterface.close!(stmt)\nDBInterface.close!(conn)\n\nPostgres.command_tag(result) and Postgres.rows_affected(result) expose PostgreSQL command completion metadata.","title":"StructUtils results"},{"category":"section","location":"#Transactions","page":"Home","text":"using Postgres, DBInterface\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\")\nPostgres.transaction(conn) do tx\n    DBInterface.execute(tx, \"CREATE TEMP TABLE tx_demo (id int)\")\n    DBInterface.execute(tx, \"INSERT INTO tx_demo VALUES (1)\")\nend\nPostgres.@transaction conn begin\n    DBInterface.execute(conn, \"INSERT INTO tx_demo VALUES (2)\")\nend\nDBInterface.close!(conn)","title":"Transactions"},{"category":"section","location":"#COPY-protocol","page":"Home","text":"using Postgres, DBInterface\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\")\nDBInterface.execute(conn, \"CREATE TEMP TABLE copy_demo (id int, name text)\")\nPostgres.copy_from(conn, \"COPY copy_demo (id, name) FROM STDIN\", \"1\\talpha\\n2\\tbeta\\n\")\nbytes = Postgres.copy_to(conn, \"COPY copy_demo TO STDOUT (FORMAT BINARY)\")\nPostgres.copy_from(conn, \"COPY copy_demo FROM STDIN (FORMAT BINARY)\", bytes)\nDBInterface.close!(conn)","title":"COPY protocol"},{"category":"section","location":"#LISTEN/NOTIFY","page":"Home","text":"using Postgres, DBInterface\nlistener = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\")\nnotifier = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\")\nPostgres.listen!(listener, \"events\")\nPostgres.notify!(notifier, \"events\", \"hello\")\nnotice = Postgres.wait_for_notification(listener; timeout=5.0)\n@show notice.channel notice.payload\nDBInterface.close!(notifier)\nDBInterface.close!(listener)","title":"LISTEN/NOTIFY"},{"category":"section","location":"#Cursor-streaming","page":"Home","text":"using Postgres, DBInterface\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\")\ncur = Postgres.cursor(conn, \"SELECT generate_series(1, 5) AS n\"; fetchsize=2)\nfor row in cur\n    @show row.n\nend\nDBInterface.close!(cur)\nDBInterface.close!(conn)","title":"Cursor streaming"},{"category":"section","location":"#Type-registry","page":"Home","text":"using Postgres, DBInterface, Tables\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\")\nDBInterface.execute(conn, \"CREATE TYPE mood AS ENUM ('sad', 'ok', 'happy')\")\nPostgres.register_enum!(conn, \"mood\")\nrow = only(Tables.rowtable(DBInterface.execute(conn, \"SELECT 'happy'::mood AS mood\")))\n@show row.mood\nDBInterface.close!(conn)\n\nNumeric values are represented by Postgres.Numeric, interval values by Dates.Period or Dates.CompoundPeriod, and range types by Postgres.PostgresRange{T}.","title":"Type registry"},{"category":"section","location":"#Query-logging","page":"Home","text":"Query logging (and other driver behavior) is customized with a driver style; see the Manual for details.\n\nusing Postgres, DBInterface\n\nstruct LoggingStyle <: Postgres.AbstractPostgresStyle end\nPostgres.query_logging_enabled(::LoggingStyle) = true\nPostgres.query_logger(::LoggingStyle, event::Symbol, info::NamedTuple) = @info \"query\" event info.success info.duration_ns\n\nconn = DBInterface.connect(Postgres.Connection, \"host=127.0.0.1;user=postgres;password=postgres;dbname=postgres\"; style=LoggingStyle())\nDBInterface.execute(conn, \"SELECT 1\")\nDBInterface.close!(conn)","title":"Query logging"},{"category":"section","location":"#Connection-pooling","page":"Home","text":"using Postgres, DBInterface\npool = Postgres.ConnectionPool(Postgres.Connection, \"127.0.0.1\", \"postgres\", \"postgres\"; dbname=\"postgres\", limit=5)\nPostgres.with_connection(pool) do conn\n    DBInterface.execute(conn, \"SELECT 1\")\nend\nDBInterface.close!(pool)","title":"Connection pooling"},{"category":"section","location":"#Errors-and-cancellation","page":"Home","text":"Postgres.Error includes SQLSTATE information. Use Postgres.cancel_query!(conn) to cancel a running query.","title":"Errors and cancellation"},{"category":"section","location":"#Reference","page":"Home","text":"","title":"Reference"},{"category":"type","location":"#Postgres.Connection","page":"Home","text":"Postgres.Connection\n\nA single connection to a PostgreSQL server, created via DBInterface.connect(Postgres.Connection, ...):\n\nDBInterface.connect(Postgres.Connection, host, user, password; dbname, port=5432, kwargs...)\nDBInterface.connect(Postgres.Connection, dsn::String; kwargs...)\nDBInterface.connect(Postgres.Connection, params::ConnectionParams; kwargs...)\n\ndsn may be a libpq-style keyword string (\"host=127.0.0.1 user=postgres dbname=postgres\") or a PostgreSQL URI (\"postgresql://user:pass@host:5432/dbname?sslmode=require\").\n\nSupported keyword arguments. All are also available as DSN/URI options except style, which is Julia-only:\n\ndbname, port, application_name\nconnect_timeout (seconds), statement_timeout (milliseconds)\nsslmode (\"disable\", \"prefer\" (default), \"require\", \"verify-full\"), sslrootcert, sslcert, sslkey, sslcapath, and sslservername. Only verify-full verifies the server's certificate; require encrypts without authenticating the server, and the default prefer falls back to an unencrypted connection if the server declines TLS. sslcapath is a fallback CA bundle or directory used only when sslrootcert is unset (it is ignored otherwise). sslservername overrides the TLS server name when the host is a pre-resolved address — note that under verify-full this is also the name the certificate is verified against, so it must name the server you intend to authenticate.\nstatement_cache_maxsize: LRU backend cache size for explicit named prepared statements (default 100; 0 disables)\nreconnect: automatically reconnect and re-prepare statements if the connection is found dead (default false; never reconnects mid-transaction)\nstyle: a custom AbstractPostgresStyle for query logging / notice / notification behavior\ndebug: log wire protocol messages. Authentication messages are redacted, but bind parameter values are not — treat a debug log as sensitive as the data the connection carries.\n\nIndividual operations are serialized on an internal lock. Keep each manual transaction and streaming cursor on one task and do not run unrelated work on that connection until the scope ends. Use a ConnectionPool for concurrent work. cancel_query! is the intentional cross-task exception. Close with DBInterface.close!(conn) or close(conn); the do-block form DBInterface.connect(f, Postgres.Connection, ...) closes automatically.\n\n\n\n\n\n","title":"Postgres.Connection"},{"category":"type","location":"#Postgres.ConnectionPool","page":"Home","text":"Postgres.ConnectionPool\n\nA pool of Connections, created lazily up to limit and reused across acquire/release cycles. Locally closed connections are replaced. A peer close that the local socket has not observed can surface on the borrower's first operation; an ambiguous failed operation is never retried automatically.\n\nConnectionPool(Postgres.Connection, host, user, password; limit=10, kwargs...)\nConnectionPool(dsn::String; limit=10, kwargs...)\nConnectionPool(params::ConnectionParams; limit=10, kwargs...)\nConnectionPool(connector::Function; limit=10)\n\nPrefer with_connection over manual acquire/release. Close all pooled connections with DBInterface.close!(pool).\n\n\n\n\n\n","title":"Postgres.ConnectionPool"},{"category":"type","location":"#Postgres.PostgresInterfaceError","page":"Home","text":"Postgres.PostgresInterfaceError <: Exception\n\nA client-side error raised by Postgres.jl itself (closed connections, parameter-count mismatches, unsupported features, ...), as opposed to Postgres.Error, which represents an error reported by the server.\n\n\n\n\n\n","title":"Postgres.PostgresInterfaceError"},{"category":"method","location":"#Postgres.acquire-Tuple{Postgres.ConnectionPool}","page":"Home","text":"Postgres.acquire(pool; forcenew=false) -> Connection\n\nTake a connection from the pool, creating one if none is available (blocking if the pool is at its limit). Return it with release.\n\n\n\n\n\n","title":"Postgres.acquire"},{"category":"method","location":"#Postgres.cancel_query!-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.cancel_query!(conn)\n\nSend a PostgreSQL CancelRequest for the query currently running on conn (over a separate, short-lived connection, so it works while conn is busy). The cancelled query fails with a Postgres.Error with SQLSTATE 57014.\n\nThe cancel connection uses the same TLS settings as conn, since the cancel key it carries is a credential: if conn itself is on TLS, the cancel connection requires TLS too. Throws a PostgresInterfaceError if the cancel request could not be delivered (rather than failing silently, which would leave the query running).\n\n\n\n\n\n","title":"Postgres.cancel_query!"},{"category":"method","location":"#Postgres.clear_statement_cache!-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.clear_statement_cache!(conn)\n\nClose all server-side prepared statements in the connection's cache and empty it.\n\n\n\n\n\n","title":"Postgres.clear_statement_cache!"},{"category":"method","location":"#Postgres.command_tag-Tuple{Postgres.Result}","page":"Home","text":"Postgres.command_tag(result) -> Union{String, Nothing}\n\nThe PostgreSQL command completion tag for the executed statement, e.g. \"SELECT 5\", \"INSERT 0 2\", or \"UPDATE 3\".\n\n\n\n\n\n","title":"Postgres.command_tag"},{"category":"method","location":"#Postgres.commit-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.commit(conn)\n\nCommit the current transaction (or release one level of transaction nesting).\n\n\n\n\n\n","title":"Postgres.commit"},{"category":"method","location":"#Postgres.copy_from-Tuple{Postgres.Connection, AbstractString, IO}","page":"Home","text":"Postgres.copy_from(conn, sql, data)\n\nExecute a COPY ... FROM STDIN statement, streaming data (an IO, string, or byte vector) to the server. Supports all COPY formats, including (FORMAT BINARY). Returns conn.\n\n\n\n\n\n","title":"Postgres.copy_from"},{"category":"method","location":"#Postgres.copy_to-Tuple{Postgres.Connection, AbstractString, IO}","page":"Home","text":"Postgres.copy_to(conn, sql, [dest::IO])\n\nExecute a COPY ... TO STDOUT statement. With a dest IO, the copy stream is written to it and dest is returned; without one, the raw bytes are returned as a Vector{UInt8}.\n\n\n\n\n\n","title":"Postgres.copy_to"},{"category":"function","location":"#Postgres.cursor","page":"Home","text":"Postgres.cursor(conn, sql, params=nothing; fetchsize=1000) -> Cursor\nPostgres.cursor(stmt, params=nothing; fetchsize=1000) -> Cursor\n\nExecute a query and stream its result rows in batches of fetchsize instead of materializing them all at once. The returned cursor iterates rows; close it with DBInterface.close!(cursor). A cursor requires a transaction: one is started (and committed on close) if the connection isn't already in one.\n\n\n\n\n\n","title":"Postgres.cursor"},{"category":"method","location":"#Postgres.describe-Tuple{Postgres.Connection, AbstractString}","page":"Home","text":"Postgres.describe(conn, table; schema=\"public\")\n\nReturn a printable summary of a table's columns: name, type, nullability, default, primary-key flag, and foreign-key reference.\n\n\n\n\n\n","title":"Postgres.describe"},{"category":"method","location":"#Postgres.escape_identifier-Tuple{AbstractString}","page":"Home","text":"Postgres.escape_identifier(name) -> String\n\nQuote a string for use as a SQL identifier (double-quoted, embedded quotes doubled). Throws if name contains a NUL byte.\n\n\n\n\n\n","title":"Postgres.escape_identifier"},{"category":"method","location":"#Postgres.escape_literal-Tuple{AbstractString}","page":"Home","text":"Postgres.escape_literal(val) -> String\n\nQuote a string for use as a SQL literal (single-quoted, embedded quotes doubled). Throws if val contains a NUL byte.\n\nPrefer query parameters ($1, $2, ...) over literal interpolation whenever possible — parameters are never parsed as SQL. Inputs containing a backslash use PostgreSQL's explicit escape-string syntax, with backslashes and quotes escaped so the result is independent of standard_conforming_strings.\n\n\n\n\n\n","title":"Postgres.escape_literal"},{"category":"method","location":"#Postgres.get_cached_statements-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.get_cached_statements(conn) -> Dict{String, Statement}\n\nReturn a copy of the connection's prepared-statement cache, keyed by SQL text.\n\n\n\n\n\n","title":"Postgres.get_cached_statements"},{"category":"method","location":"#Postgres.get_server_parameter-Tuple{Postgres.Connection, String}","page":"Home","text":"Postgres.get_server_parameter(conn, name) -> Union{String, Nothing}\n\nReturn the server-reported value of runtime parameter name (e.g. \"server_version\", \"TimeZone\"), or nothing if the server has not reported it.\n\n\n\n\n\n","title":"Postgres.get_server_parameter"},{"category":"method","location":"#Postgres.get_server_parameters-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.get_server_parameters(conn) -> Dict{String, String}\n\nReturn a copy of all runtime parameters the server has reported on this connection.\n\n\n\n\n\n","title":"Postgres.get_server_parameters"},{"category":"method","location":"#Postgres.get_statement_timeout-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.get_statement_timeout(conn) -> Union{Int, Nothing}\n\nReturn the statement timeout (milliseconds) configured on the connection, or nothing if none was set.\n\n\n\n\n\n","title":"Postgres.get_statement_timeout"},{"category":"method","location":"#Postgres.in_transaction-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.in_transaction(conn) -> Bool\n\nWhether the connection currently has an open transaction.\n\n\n\n\n\n","title":"Postgres.in_transaction"},{"category":"method","location":"#Postgres.listen!-Tuple{Postgres.Connection, AbstractString}","page":"Home","text":"Postgres.listen!(conn, channel)\n\nExecute LISTEN channel so the connection receives notifications for channel. Use wait_for_notification to block until one arrives.\n\n\n\n\n\n","title":"Postgres.listen!"},{"category":"function","location":"#Postgres.notify!","page":"Home","text":"Postgres.notify!(conn, channel, payload=nothing)\n\nExecute NOTIFY channel (with optional payload), delivering a Notification to all connections listening on channel.\n\n\n\n\n\n","title":"Postgres.notify!"},{"category":"method","location":"#Postgres.register_composite!-Tuple{Postgres.Connection, AbstractString}","page":"Home","text":"Postgres.register_composite!(conn, name; schema=\"public\")\n\nLook up the composite type schema.name on the server and register it so values are returned as NamedTuples with the composite's field names.\n\n\n\n\n\n","title":"Postgres.register_composite!"},{"category":"method","location":"#Postgres.register_enum!-Tuple{Postgres.Connection, AbstractString}","page":"Home","text":"Postgres.register_enum!(conn, name; schema=\"public\", julia_type=Symbol)\n\nLook up the enum type schema.name on the server and register it so values are returned as julia_type (by default Symbol). The supported types are Symbol and String.\n\n\n\n\n\n","title":"Postgres.register_enum!"},{"category":"method","location":"#Postgres.register_range!-Tuple{Postgres.Connection, AbstractString}","page":"Home","text":"Postgres.register_range!(conn, name; schema=\"public\")\n\nLook up the range type schema.name on the server and register it so values are returned as PostgresRange of the range's element type.\n\nThe element type is captured when the range is registered, so register it first: a range over a custom enum or composite must come after the corresponding register_enum! / register_composite! call.\n\n\n\n\n\n","title":"Postgres.register_range!"},{"category":"method","location":"#Postgres.register_type!-Tuple{Postgres.Connection, Integer, Type}","page":"Home","text":"Postgres.register_type!(conn, oid, julia_type; parser=nothing)\n\nRegister a mapping from PostgreSQL type oid to julia_type in the connection's type registry. parser is a (val::String, registry) -> value function that converts the wire text. If parser is omitted, julia_type must be String. See also register_enum!, register_composite!, and register_range!.\n\n\n\n\n\n","title":"Postgres.register_type!"},{"category":"method","location":"#Postgres.release-Tuple{Postgres.ConnectionPool, Postgres.Connection}","page":"Home","text":"Postgres.release(pool, conn)\n\nReturn a connection previously taken with acquire to the pool. A connection still inside a transaction is rolled back first, so the next borrower starts from a clean session; if it can't be rolled back it is closed rather than reused.\n\n\n\n\n\n","title":"Postgres.release"},{"category":"method","location":"#Postgres.rollback-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.rollback(conn)\n\nRoll back the current transaction (or, in a nested transaction, roll back to the enclosing savepoint).\n\n\n\n\n\n","title":"Postgres.rollback"},{"category":"method","location":"#Postgres.rows_affected-Tuple{Postgres.Result}","page":"Home","text":"Postgres.rows_affected(result) -> Union{Int, Nothing}\n\nThe number of rows the statement affected (parsed from the command tag), or nothing when the statement doesn't report one.\n\n\n\n\n\n","title":"Postgres.rows_affected"},{"category":"method","location":"#Postgres.set_statement_cache_maxsize!-Tuple{Postgres.Connection, Integer}","page":"Home","text":"Postgres.set_statement_cache_maxsize!(conn, maxsize)\n\nSet the maximum number of prepared statements the connection caches (LRU eviction). 0 disables caching and closes all currently cached statements.\n\n\n\n\n\n","title":"Postgres.set_statement_cache_maxsize!"},{"category":"method","location":"#Postgres.set_statement_timeout!-Tuple{Postgres.Connection, Union{Nothing, Integer}}","page":"Home","text":"Postgres.set_statement_timeout!(conn, timeout)\n\nSet the server statement_timeout for the connection, in milliseconds. nothing or 0 disables the timeout. This setting is session state and cannot be changed while a transaction is open. An explicit disable is retained across automatic reconnects as 0.\n\n\n\n\n\n","title":"Postgres.set_statement_timeout!"},{"category":"method","location":"#Postgres.start_transaction-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.start_transaction(conn)\n\nBegin a transaction (BEGIN). If a transaction is already open, create a savepoint instead, so transactions nest. Pair with commit or rollback; prefer transaction or @transaction for automatic handling.\n\n\n\n\n\n","title":"Postgres.start_transaction"},{"category":"method","location":"#Postgres.transaction-Union{Tuple{F}, Tuple{F, Postgres.Connection}} where F","page":"Home","text":"Postgres.transaction(f, conn)\n\nRun f(conn) inside a transaction: committed if f returns normally, rolled back if it throws. Nested calls use savepoints. Returns f's result.\n\nA transaction already opened with raw SQL (execute(conn, \"BEGIN\")) is treated as the enclosing level: the block nests inside it with a savepoint and leaves it open, so the caller's own COMMIT/ROLLBACK stays in control of their transaction.\n\nPostgres.transaction(conn) do conn\n    DBInterface.execute(conn, \"INSERT INTO t VALUES (1)\")\nend\n\n\n\n\n\n","title":"Postgres.transaction"},{"category":"method","location":"#Postgres.unlisten!-Tuple{Postgres.Connection, AbstractString}","page":"Home","text":"Postgres.unlisten!(conn, channel)\n\nExecute UNLISTEN channel to stop receiving notifications for channel.\n\n\n\n\n\n","title":"Postgres.unlisten!"},{"category":"method","location":"#Postgres.wait_for_notification-Tuple{Postgres.Connection}","page":"Home","text":"Postgres.wait_for_notification(conn; timeout=nothing) -> Union{Notification, Nothing}\n\nBlock until a NOTIFY message arrives on the connection (see listen!) and return it as a Notification. With a timeout (seconds), return nothing if no message begins arriving in that window; once a message starts, it is always read to completion so the connection is never left parked mid-message. The connection lock is held while waiting, so use a dedicated connection for listening — that is also the only way to receive every notification, since notifications that arrive while the connection is busy with a query are delivered to notification_callback only during the phases of a query that read result data.\n\nOver TLS the poll interval bounds a read on the underlying transport rather than on the TLS record layer, so a record that arrives split across a poll boundary cannot be resumed. That is detected on the following poll and closes the connection with an error rather than returning corrupt data; a blocking wait (no timeout) is not affected.\n\n\n\n\n\n","title":"Postgres.wait_for_notification"},{"category":"method","location":"#Postgres.with_connection-Tuple{Function, Postgres.ConnectionPool}","page":"Home","text":"Postgres.with_connection(f, pool; forcenew=false)\n\nAcquire a connection from the pool, call f(conn), and release the connection back to the pool afterwards. Returns f's result.\n\n\n\n\n\n","title":"Postgres.with_connection"},{"category":"macro","location":"#Postgres.@transaction-Tuple{Any, Any}","page":"Home","text":"Postgres.@transaction conn expr\n\nRun expr inside a transaction. Any non-exceptional exit commits: normal completion, return (which then returns from the enclosing function), break, or continue. Only a thrown exception rolls back. Evaluates to expr's value. Nested @transaction blocks use savepoints, and an early return commits every enclosing level on its way out.\n\nThe body keeps plain Julia semantics: a return inside a nested function, closure, do-block, or any task-forming macro (Threads.@spawn, @async, Distributed.@spawnat, third-party equivalents) belongs to that function or task, exactly as it would outside the macro.\n\n\n\n\n\n","title":"Postgres.@transaction"},{"category":"type","location":"#Postgres.API.Error","page":"Home","text":"Postgres.Error <: Exception\n\nA PostgreSQL server error (an ErrorResponse message). Carries the fields the server reported: severity (non-localized when the server supplies it, so it can be compared against \"FATAL\", \"ERROR\", ... regardless of the server's lc_messages), code (the SQLSTATE, e.g. \"23505\"), message, and optional context such as detail, hint, position, schema, table, column, and constraint. A small number of protocol-level failures detected client-side (unsupported authentication methods, protocol desync) also use this type, with an empty code; other client-side failures throw Postgres.PostgresInterfaceError.\n\n\n\n\n\n","title":"Postgres.API.Error"},{"category":"type","location":"#Postgres.API.Notification","page":"Home","text":"Postgres.Notification\n\nAn asynchronous NOTIFY message received from the server, with the notifying backend's pid, the channel name, and the payload string (empty when the notification had no payload). See Postgres.listen! and Postgres.wait_for_notification.\n\n\n\n\n\n","title":"Postgres.API.Notification"},{"category":"type","location":"#Postgres.API.Numeric","page":"Home","text":"Postgres.Numeric\n\nExact decimal representation of a PostgreSQL numeric/decimal value: coeff * 10^-scale, where coeff is a BigInt and scale the number of digits after the decimal point. Preserves the value and scale exactly (no floating-point rounding). print/string produce the decimal text form.\n\nThe PostgreSQL special values NaN, Infinity, and -Infinity cannot be represented and throw an error when encountered.\n\n\n\n\n\n","title":"Postgres.API.Numeric"},{"category":"type","location":"#Postgres.API.PostgresRange","page":"Home","text":"Postgres.PostgresRange{T}\n\nA PostgreSQL range value (int4range, numrange, tstzrange, ...). lower and upper are the bounds (missing when unbounded), lower_inclusive and upper_inclusive indicate whether each bound is inclusive, and empty is true for the empty range.\n\n\n\n\n\n","title":"Postgres.API.PostgresRange"},{"category":"type","location":"#Postgres.ConnectionString.ConnectionParams","page":"Home","text":"Postgres.ConnectionParams(; host=\"localhost\", port=5432, user=\"\", password=nothing,\n                          dbname=\"\", kwargs...)\n\nStructured connection options, an alternative to DSN strings:\n\nparams = Postgres.ConnectionParams(host=\"127.0.0.1\", user=\"postgres\", dbname=\"postgres\")\nconn = DBInterface.connect(Postgres.Connection, params)\n\nAlso produced by Postgres.parse_dsn. Supported keyword arguments mirror the connection keywords: application_name, connect_timeout, sslmode, sslrootcert, sslcert, sslkey, sslcapath, sslservername, statement_timeout, statement_cache_maxsize, debug, and reconnect.\n\n\n\n\n\n","title":"Postgres.ConnectionString.ConnectionParams"},{"category":"function","location":"#Postgres.ConnectionString.parse_dsn","page":"Home","text":"Postgres.parse_dsn(dsn) -> ConnectionParams\n\nParse a libpq-style keyword string (\"host=127.0.0.1 user=postgres\") or a PostgreSQL URI (\"postgresql://user:pass@host:5432/dbname\") into ConnectionParams. Unset options fall back to the PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE, PGAPPNAME, PGCONNECT_TIMEOUT, and PGSSL* environment variables, then to defaults.\n\n\n\n\n\n","title":"Postgres.ConnectionString.parse_dsn"}]
}
